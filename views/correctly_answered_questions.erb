<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashes & Dots - Admin Panel</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700;900&display=swap" rel="stylesheet">
  <link href="/css/output.css" rel="stylesheet">
  <link href="icon.ico" rel="icon">
</head>
<section class="flex h-screen justify-center items-center px-16 py-12 text-base font-medium leading-6 text-black max-md:px-5 bg-gradient-to-b from-purple-800 to-gray-300">
  <div class="flex flex-col justify-around px-20 py-20 max-w-full bg-white rounded-3xl shadow-sm max-md:px-5 h-full pointer-events-auto overflow-scroll">
      <button onclick="window.location.href = '/admin'"  class=" w-min bg-purple-500 text-white p-2 rounded hover:transition-all transition duration-200 hover:ease-in hover:scale-105 hover:bg-purple-700"><svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6"> <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5 3 12m0 0 7.5-7.5M3 12h18" /></svg></button>
      <h1 class="self-center mt-8 text-3xl font-extrabold">Preguntas más respondidas de forma correcta</h1>
        <% sorted_questions = @questions.sort_by { |question| -question.times_answered_correctly } %>
        <% count = 1 %>
        <% sorted_questions.each do |question| %>
            <div class="flex gap-5 text-left justify-between py-4 px-5 border-t border-solid border-neutral-200 max-md:flex-wrap max-md:pr-5">
                <span class="text-zinc-500"><%= count %></span>
                <span class="text-ellipsis max-md:max-w-full w-[80%]"><%= question.description %></span>
                <span class="text-zinc-500">De: <%= question.level.name%></span>
                <div class="flex flex-col justify-center self-stretch text-xs font-semibold leading-4">
                    <span class="justify-center px-2 py-1.5 bg-white rounded-lg border border-solid border-neutral-200 text-ellipsis"><%= question.times_answered_correctly %></span>
                </div>
            </div>
            <% count += 1 %>
        <% end %>      

  </div>
</section>