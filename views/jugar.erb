<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seleccionar Nivel</title>
    <!-- Vinculación del archivo CSS -->
    <link rel="stylesheet" type="text/css" href="/css/jugar.css">
    <!-- Resto de las etiquetas meta y de enlace -->
</head>
<body>
    <div class="container">
        <h1 class="title">Selección de Niveles</h1>
        <div class="levels-container">
            <% @levels.each_with_index do |level, index| %>
                <% if index == 0 || @user.levels.include?(@levels[index - 1]) %>
                    <div class="level <%= 'completed' if @user.levels.include?(level) %>" onclick="location.href='/nivel/<%= level.id %>'" data-level="Nivel <%= level.id %>">
                        Nivel <%= level.id %>
                    </div>
                <% else %>
                    <div class="level locked" data-level="Nivel <%= level.id %>">
                        Nivel <%= level.id %>
                    </div>
                <% end %>
            <% end %>
        </div>
    </div>
</body>
</html>
